<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.room.finder.mapper.EnquiryMapper">
    <insert id="makeEnquiry" parameterType="com.room.finder.model.Enquiry" useGeneratedKeys="true" keyProperty="id">
    insert into enquiry(enquiry_text,room_id,customer_id)values (#{enquiryText},#{roomId},#{customerId})
    </insert>
</mapper>